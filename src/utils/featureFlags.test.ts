/** * Feature Flags Utility Tests *  * Tests the feature flag system for mobile payment rollouts */import { shouldShowMobilePayments, getMobilePaymentConfig } from '@/utils/featureFlags';// Mock the environment validation modulejest.mock('@/utils/envValidation', () => ({  env: {    mobilePayments: {      enabled: true,      applePay: true,      googlePay: true,      rolloutPercentage: 100,    },  },}));describe('Feature Flag Utilities', () => {  describe('shouldShowMobilePayments', () => {    it('should return true for enabled flag with 100% rollout', () => {      const result = shouldShowMobilePayments('test-user');      expect(result).toBe(true);    });    it('should consistently return same result for same user', () => {      const userId = 'consistent-test-user';      const result1 = shouldShowMobilePayments(userId);      const result2 = shouldShowMobilePayments(userId);      expect(result1).toBe(result2);    });  });  describe('getMobilePaymentConfig', () => {    it('should return enabled config when flags are enabled', () => {      const config = getMobilePaymentConfig('test-user');      expect(config).toEqual({        enabled: true,        applePay: true,        googlePay: true,        rolloutPercentage: 100,      });    });  });});